# Build output
bin/
obj/

# Auto-cached build reference DLLs (BepInEx core) — see build/locate-game.ps1
.deps/

# IDE / editor
.vs/
.vscode/
*.user
*.suo

# OS
Thumbs.db
desktop.ini
.DS_Store

# Design plans / potential improvements (not committed — see CLAUDE.md "Plans")
plans/

# Local Claude Code hooks + settings (machine-specific deploy paths)
.claude/settings.local.json
.claude/hooks/
.claude/worktrees/

# Flight recordings + python cache (local debug data). Capture folders under debugtests/ are
# per-session dumps (CSV + LogOutput.log + anomaly logs) — local only, and that includes
# debugtests/archive/ (index-captures.py --archive). The tools at debugtests/ root ARE committed:
# check-architecture.py (release gate + Stop hook), analyze-wobble.py, scorecard.py and the rest.
# (replay-pitcheff.py used to be named here; deleted in v0.96 — pEff is a CSV column now.)
debugtests/*/
debugtests/*.csv
__pycache__/
