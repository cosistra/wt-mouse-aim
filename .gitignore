# Build output
bin/
obj/

# Auto-cached build reference DLLs (BepInEx core) — see build/locate-game.ps1
.deps/

# IDE / editor
.vs/
.vscode/
*.user
*.suo

# OS
Thumbs.db
desktop.ini
.DS_Store

# Design plans / potential improvements (not committed — see CLAUDE.md "Plans")
plans/

# Local Claude Code hooks + settings (machine-specific deploy paths)
.claude/settings.local.json
.claude/hooks/
.claude/worktrees/

# Flight recordings + python cache (local debug data; the analyzer script IS committed)
debugtests/**/*.csv
__pycache__/
